# Inline Documentation: pidog/actions_dictionary.py

This file contains significant inline documentation and comments extracted from `pidog/actions_dictionary.py`.

## Line 224

```
        # amplitude = 50
        # angs = []
        # for i in range(21):
        #     a = round(sin(i*0.314), 2)
        #     angs.append([amplitude*a])
```

## Line 235

```
        # amplitude = 20
        # angs = []
        # for i in range(20):
        #     y = round(sin(i*0.314),3)
        #     y1 = amplitude*sin(i*0.314)
        #     if y == -1 or y == 1:
        #         for _ in range(10):
        #             angs.append([0,0,y1])
        #     angs.append([0,0,y1])
        # return angs,'head'
```

